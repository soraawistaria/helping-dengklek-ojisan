#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;

    int A[N+1];
    for (int i=0; i<=N; i++) {
        cin >> A[i];
    }

    for (int i=1; i<=N; i++) {
        if(A[i+1] == A[i] + 1) {
            
        }
    }
}